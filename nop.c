#include "monty.h"

/**
* _nop - Does nothing
*
* @stack: Double pointer to the top of the stack
* @line_number: The line number where the nop opcode is located
*/
void _nop(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
